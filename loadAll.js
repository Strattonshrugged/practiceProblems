// the only javascript called in the html
// it should disregard itself, the index.html, and the readme
// all js files should be loaded as scripts
// all css files should be loaded as stylesheets

// UNFINISHED










// REFERENCE ONLY, NOT MY WORK BELOW THIS LINE

// function getFiles(dir){
//     fileList = [];
//     var files = fs.readdirSync(dir);
//     for(var i in files){
//         if (!files.hasOwnProperty(i)) continue;
//         var name = dir+'/'+files[i];
//         if (!fs.statSync(name).isDirectory()){
//             fileList.push(name);
//         }
//     }
//     return fileList;
// };
// getFiles();
